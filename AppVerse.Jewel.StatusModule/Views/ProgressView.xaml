<UserControl x:Class="AppVerse.Jewel.StatusModule.Views.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppVerse.Jewel.StatusModule.Views"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="10*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="10*" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="1" Content="{Binding StatusOf.ProgressOf}"/>
        <mah:MetroProgressBar  Value="{Binding StatusOf.Progress}" Maximum="{Binding StatusOf.Max}" Grid.Column="3"/>
    </Grid>
</UserControl>
