<UserControl x:Class="AppVerse.Jewel.HorizonModule.Views.DepthFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppVerse.Jewel.HorizonModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2"/>
            <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2"/>
            <ColumnDefinition Width="2.5*"/>
            <ColumnDefinition Width="2"/>
            <ColumnDefinition Width="2.5*"/>
                    <ColumnDefinition Width="2"/>
        </Grid.ColumnDefinitions>
                <CheckBox  Grid.Column="0" IsChecked="{Binding IsLoaded}"/>
                <TextBlock Margin="0,0,5,0" HorizontalAlignment="Left" Grid.Column="2" Text="{Binding Path=FileLoadProgress.ProgressOf}" />
                <ContentControl  Grid.Column="4" Content="{Binding Path=FileLoadProgress}"/>
        <Button Grid.Column="6" Command="{Binding LoadFilesCommand}" Content="{Binding LoadStatus}"/>
            </Grid>
    
    
</UserControl>
